"""Deterministic job-corpus generator (generation side, never imported by
workflows). `python -m forja.bench.corpusgen.generator --size 10000` writes
benchmark_data/jobs_v2.json (workflow-visible) and
benchmark_data/manifest.json (ground truth — eval side only)."""
