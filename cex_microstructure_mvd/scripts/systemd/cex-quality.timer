[Unit]
Description=Run CEX data-quality report daily

[Timer]
# Every day at 00:10 UTC, plus ~2min jitter; catch up if the host was asleep.
OnCalendar=*-*-* 00:10:00 UTC
RandomizedDelaySec=120
Persistent=true

[Install]
WantedBy=timers.target
